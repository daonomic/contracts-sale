pragma solidity ^0.5.0;

contract PaymentMethods {
    address constant ETH = address(0);
    address constant USD = address(1);
    address constant BTC = address(2);
    address constant LTC = address(3);
    address constant BCH = address(4);
}
